---
phase: 06-agent-spawn-hook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-03
  - HOOK-04
  - HOOK-05
  - HOOK-06
must_haves:
  truths:
    - ".claude/hooks/pre-agent.sh exists and logs timestamp + agent type + description (HOOK-01 — pre-satisfied)"
    - ".claude/settings.json has PreToolUse matcher for Agent triggering pre-agent.sh (HOOK-02 — pre-satisfied)"
    - "Agent spawns are logged to .claude/workspace/agent-log.txt (HOOK-03 — pre-satisfied)"
    - "pre-agent.sh always exits 0 via trap (HOOK-04 — pre-satisfied)"
    - "Session-gate Check 21 counts spawns and alerts when count exceeds 15 (HOOK-05 — pre-satisfied, REQUIREMENTS.md reference corrected)"
    - "session-start.sh clears agent-log.txt so counter resets each session (HOOK-06 — pre-satisfied)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected check reference (Check 21, not Check 19)"
      contains: "Check 21"
---

<objective>
Verify all 6 HOOK requirements are satisfied and fix the Check 19 → Check 21 reference in REQUIREMENTS.md.

All Phase 6 artifacts were built during Track A. This plan validates they exist and corrects the check numbering mismatch (same pattern as Phase 4's Check 18 → Check 20 fix).

Output: Updated REQUIREMENTS.md with corrected check reference.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-agent-spawn-hook/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix REQUIREMENTS.md Check 19 → Check 21 and validate all 6 HOOK requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines containing HOOK-05 — the line to fix)
    - .claude/skills/session-gate/SKILL.md (lines 298-307 — verify Check 21 exists)
    - .claude/hooks/pre-agent.sh (verify HOOK-01/03/04)
    - .claude/settings.json (verify HOOK-02 Agent matcher)
    - .claude/hooks/session-start.sh (verify HOOK-06 agent-log clear)
  </read_first>
  <action>
In `.planning/REQUIREMENTS.md`, change the 1 occurrence of "Check 19" in HOOK-05 to "Check 21":

- `Session-gate Check 19 (END, informational)` → `Session-gate Check 21 (END, informational)`

Then validate all 6 requirements with grep:

1. HOOK-01: `test -f .claude/hooks/pre-agent.sh` — exists
2. HOOK-02: `grep "Agent" .claude/settings.json` — matcher present
3. HOOK-03: `grep "agent-log.txt" .claude/hooks/pre-agent.sh` — log path present
4. HOOK-04: `grep "exit 0" .claude/hooks/pre-agent.sh` — trap present
5. HOOK-05: `grep "Check 21.*Agent spawn" .claude/skills/session-gate/SKILL.md` — check exists
6. HOOK-06: `grep "agent-log" .claude/hooks/session-start.sh` — clear present
  </action>
  <verify>
    <automated>cd "/Users/gerardvinou/Claude code/Claude Projects/project-template-v2" && grep "Check 21" .planning/REQUIREMENTS.md | head -2 && grep -c "Check 19" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md HOOK-05 says "Check 21" (not "Check 19")
    - grep -c "Check 19" .planning/REQUIREMENTS.md returns 0
    - All 6 HOOK artifacts exist and pass grep validation
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md corrected from Check 19 to Check 21. All 6 HOOK requirements verified as pre-satisfied from Track A.
  </done>
</task>

</tasks>

<verification>
| ID | Check | Method |
|----|-------|--------|
| HOOK-01 | pre-agent.sh exists with logging | `test -f .claude/hooks/pre-agent.sh && grep "timestamp\|TIMESTAMP" .claude/hooks/pre-agent.sh` |
| HOOK-02 | settings.json Agent matcher | `grep "Agent" .claude/settings.json` |
| HOOK-03 | Log to agent-log.txt | `grep "agent-log.txt" .claude/hooks/pre-agent.sh` |
| HOOK-04 | Always exit 0 | `grep "exit 0" .claude/hooks/pre-agent.sh` |
| HOOK-05 | Check 21 in session-gate | `grep "Check 21" .claude/skills/session-gate/SKILL.md` |
| HOOK-06 | session-start.sh clears log | `grep "agent-log" .claude/hooks/session-start.sh` |
</verification>

<success_criteria>
- All 6 HOOK artifacts exist and pass grep validation
- REQUIREMENTS.md says Check 21 (not Check 19)
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-spawn-hook/06-01-SUMMARY.md`
</output>
