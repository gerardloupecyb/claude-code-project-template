---
phase: 03-sparc-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sparc/SKILL.md
autonomous: true
requirements: [SPARC-01, SPARC-02, SPARC-03, SPARC-04, SPARC-05, SPARC-06, SPARC-07, SPARC-08, SPARC-09]

must_haves:
  truths:
    - "Phase 5 (Complete) spawns reviewer + critic agents in parallel following swarm-patterns.md routing"
    - "Every phase transition requires explicit user validation before advancing (including Phase 4 to Phase 5)"
    - "Phase 5 can be invoked standalone for review without running Phases 1-4"
    - "File stays under 150 lines after all edits"
  artifacts:
    - path: ".claude/skills/sparc/SKILL.md"
      provides: "5-phase SPARC micro-execution skill with dual-agent phases 3 and 5"
      contains: "reviewer.*critic.*parallel"
  key_links:
    - from: ".claude/skills/sparc/SKILL.md"
      to: ".claude/rules/swarm-patterns.md"
      via: "Phase 5 reviewer + critic model routing reference"
      pattern: "ref: .*swarm-patterns\\.md"
    - from: ".claude/skills/sparc/SKILL.md"
      to: "docs/architecture/contexts.md"
      via: "Phase 3 architect and critic constraint"
      pattern: "docs/architecture/contexts\\.md"
---

<objective>
Close 3 targeted gaps in the SPARC SKILL.md so all 9 SPARC requirements are satisfied.

The SPARC SKILL.md was built during Track A integration (125 lines) and already satisfies 6 of 9 requirements. Gap analysis reveals 3 specific issues:

1. **SPARC-03 gap**: Phase 5 currently spawns "CE:review + Codex adversarial" as its dual structure. The requirement says "reviewer + critic in parallel (tier 3)" matching Phase 3's dual-agent pattern. Codex adversarial review should become an optional add-on, not replace the critic agent.
2. **SPARC-04 gap**: Line 118 says "except Phase 5 which runs automatically after Phase 4" — this violates the requirement that EVERY phase waits for validation. The exception must be removed.
3. **SPARC-06 gap**: The entry points table mentions standalone usage but there is no explicit "/sparc:review" or "Phase 5 alone" entry point for review-only invocation.

Requirements already satisfied (no changes needed):
- SPARC-01: All 5 phases present (Spec, Pseudo, Arch, Refine, Complete)
- SPARC-02: Phase 3 dual-agent architect + critic in parallel at tier 3 (lines 55-58)
- SPARC-05: Phase 4 delegates to GSD standard execution (lines 78-79)
- SPARC-07: Workspace files written to `.claude/workspace/sparc-*.md` (lines 35, 47, 63)
- SPARC-08: File is 125 lines, well under 150
- SPARC-09: contexts.md passed to Phase 3 agents as constraint (line 60)

Purpose: Align Phase 5 with the dual-agent pattern established in Phase 3, enforce validation gates consistently, and enable standalone review.
Output: Updated `.claude/skills/sparc/SKILL.md` with 3 targeted edits.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-preflight-agent5-critic/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Key content the executor needs from swarm-patterns.md (Phase 1 output) -->

From .claude/rules/swarm-patterns.md — SPARC routing by phase (lines 43-53):
```
| Phase | Agent | Model | Reason |
|-------|-------|-------|--------|
| 5 Complete | reviewer | Sonnet | Ordinary review |
| 5 Complete | reviewer + critic | Opus | If security, contradiction, or bounded context |
```

From .claude/rules/swarm-patterns.md — Escalation triggers (lines 55-60):
```
**Escalate to Opus when:**
- Auth, security, permissions, or sensitive data
- Bounded contexts or API boundaries in scope
- Architect and critic contradict without clear resolution
- 2nd NO-GO on the same task
- Costly-to-reverse decision (schema, API contract, data model, orchestration)
```

Phase 2 established pattern (from 02-01-SUMMARY.md):
```
"Reference-based model routing: agent definitions point to swarm-patterns.md for model tier, not hardcoded values"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix 3 targeted gaps in SPARC SKILL.md</name>
  <files>.claude/skills/sparc/SKILL.md</files>
  <read_first>
    - .claude/skills/sparc/SKILL.md (the file being modified -- read in full)
    - .claude/rules/swarm-patterns.md (source of truth for Phase 5 model routing)
  </read_first>
  <action>
Make exactly 3 edits to `.claude/skills/sparc/SKILL.md`:

**Edit 1 — Rewrite Phase 5 as reviewer + critic dual-agent (SPARC-03):**

Replace the entire Phase 5 section (lines 92-110) with the following. The structure mirrors Phase 3's dual-agent pattern:

```markdown
## Phase 5 — Completion (dual review)

Spawn 2 agents IN PARALLEL (ref: swarm-patterns.md):

- `reviewer` (per swarm-patterns.md routing): reviews code against Phase 1 ACs, checks architecture, patterns, quality, maintainability
- `critic` (per swarm-patterns.md routing): challenges design approach, tradeoffs, risks, hidden costs

If escalation triggers apply (security, bounded contexts, contradiction, 2nd NO-GO): both agents run at tier 3 (Opus).

Claude principal synthesizes both outputs -> **GO / NO-GO verdict**:
- **GO**: task complete, close associated todos with `/todo close {ID}`
- **NO-GO**: return to Phase 4 with specific findings from both reviews

Optional: if Codex plugin installed, also run `/codex:adversarial-review --base main` for cross-model challenge. Skip silently if not installed.

- Write to: `.claude/workspace/sparc-review.md`
```

This replaces the previous CE:review + Codex structure with reviewer + critic per swarm-patterns.md, matching SPARC-03. Codex becomes an optional add-on.

**Edit 2 — Add validation gate between Phase 4 and Phase 5 (SPARC-04):**

At the end of Phase 4 (after the Option B Codex block, before Phase 5), add:

```markdown
**Validation**: Show implementation summary to user. Wait for confirmation before Phase 5.
```

This matches the pattern already used in Phases 1-3 (lines 37, 49, 65).

Also fix line 118 in the skip rules. Replace:
```
- Never auto-chain without user validation between phases (except Phase 5 which runs automatically after Phase 4)
```
With:
```
- Never auto-chain without user validation between phases
```

This removes the Phase 5 exception that contradicts SPARC-04.

**Edit 3 — Add standalone Phase 5 entry point (SPARC-06):**

In the Entry Points table (lines 21-24), add a third row:

```markdown
| `/sparc:review` standalone | Review-only (skip Phases 1-4) | Existing code + optional workspace files |
```

And add a note below the table (after line 25):

```markdown
**Phase 5 standalone**: `/sparc:review` runs only Phase 5 (reviewer + critic) against existing code. Useful for Codex-produced code review or post-implementation validation without running full SPARC.
```

**Do NOT change anything else.** Phases 1-4 definitions, Phase 3 dual-agent structure, workspace paths for Phases 1-3, skip rules (other than the line 118 fix), and "What SPARC Does NOT Do" section remain as-is.

**Line budget**: Current file is 125 lines. Edit 1 replaces ~19 lines with ~14 lines (-5). Edit 2 adds ~2 lines (+2) and modifies 1 existing line (0). Edit 3 adds ~3 lines (+3). Net change: ~0 lines. Final file should be around 125-130 lines, well under 150.
  </action>
  <verify>
    <automated>cd "/Users/gerardvinou/Claude code/Claude Projects/project-template-v2" && F=".claude/skills/sparc/SKILL.md" && echo "=== SPARC-03: reviewer + critic in Phase 5 ===" && grep -n "reviewer.*swarm-patterns\|critic.*swarm-patterns" "$F" | grep -i "phase 5\|completion" || grep -n "reviewer.*routing\|critic.*routing" "$F" && echo "=== SPARC-04: validation before Phase 5 ===" && grep -n "Wait for confirmation before Phase 5" "$F" && echo "=== SPARC-04: no Phase 5 exception ===" && grep -c "except Phase 5" "$F" | grep -q "^0$" && echo "PASS: exception removed" || echo "FAIL: exception still present" && echo "=== SPARC-06: standalone Phase 5 ===" && grep -n "sparc:review" "$F" && echo "=== Line count ===" && wc -l "$F"</automated>
  </verify>
  <acceptance_criteria>
    - grep "reviewer.*swarm-patterns\|per swarm-patterns.md routing" in Phase 5 section returns matches (Edit 1 reviewer routing)
    - grep "critic.*swarm-patterns\|per swarm-patterns.md routing" in Phase 5 section returns matches (Edit 1 critic routing)
    - grep "Wait for confirmation before Phase 5" returns exactly 1 match (Edit 2 validation gate)
    - grep "except Phase 5" returns 0 matches (Edit 2 exception removed)
    - grep "sparc:review" returns at least 1 match (Edit 3 standalone entry point)
    - wc -l returns a number less than 150 (SPARC-08 maintained)
    - Phase 3 dual-agent structure unchanged (architect + critic at tier 3)
    - Workspace files for Phases 1-3 unchanged (sparc-spec.md, sparc-pseudo.md, sparc-arch.md)
  </acceptance_criteria>
  <done>
    Three targeted edits applied: (1) Phase 5 rewritten as reviewer + critic dual-agent per swarm-patterns.md routing with Codex as optional add-on, (2) validation gate added between Phase 4 and Phase 5 with Phase 5 auto-chain exception removed, (3) standalone `/sparc:review` entry point added for review-only use case.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate all 9 SPARC requirements are satisfied</name>
  <files>.claude/skills/sparc/SKILL.md</files>
  <read_first>
    - .claude/skills/sparc/SKILL.md (re-read after Task 1 edits)
    - .planning/REQUIREMENTS.md (requirements to validate against)
  </read_first>
  <action>
Run grep-based validation for each SPARC requirement against the updated SKILL.md. This is a read-only validation task -- no file edits.

Check each requirement:

**SPARC-01** -- 5 phases present:
```bash
grep "## Phase 1" SKILL.md && grep "## Phase 2" SKILL.md && grep "## Phase 3" SKILL.md && grep "## Phase 4" SKILL.md && grep "## Phase 5" SKILL.md
```
Expected: all 5 match.

**SPARC-02** -- Phase 3 dual-agent architect + critic parallel tier 3:
```bash
grep "architect.*tier 3" SKILL.md && grep "critic.*tier 3" SKILL.md
```
Expected: both match in Phase 3 section.

**SPARC-03** -- Phase 5 dual-agent reviewer + critic:
```bash
grep "reviewer.*swarm-patterns\|reviewer.*routing" SKILL.md && grep "critic.*swarm-patterns\|critic.*routing" SKILL.md
```
Expected: both match in Phase 5 section (reviewer + critic per swarm-patterns.md).

**SPARC-04** -- Each phase waits for validation:
```bash
grep "Wait for confirmation before Phase 2" SKILL.md
grep "Wait for confirmation before Phase 3" SKILL.md
grep "Wait for confirmation before Phase 4" SKILL.md
grep "Wait for confirmation before Phase 5" SKILL.md
```
Expected: all 4 match. And grep "except Phase 5" returns 0 matches.

**SPARC-05** -- Phase 4 = GSD standard execution:
```bash
grep "GSD standard execution" SKILL.md
```
Expected: match.

**SPARC-06** -- Standalone without GSD:
```bash
grep "sparc:review" SKILL.md
```
Expected: match (standalone Phase 5 entry).

**SPARC-07** -- Workspace artifacts:
```bash
grep "sparc-spec.md\|sparc-pseudo.md\|sparc-arch.md\|sparc-review.md" SKILL.md
```
Expected: 4 matches (one per workspace file).

**SPARC-08** -- File under 150 lines:
```bash
wc -l SKILL.md
```
Expected: less than 150.

**SPARC-09** -- contexts.md passed as constraint in Phase 3:
```bash
grep "docs/architecture/contexts.md" SKILL.md
```
Expected: match in Phase 3 section.

Report pass/fail for each requirement. If any fail, diagnose and fix before marking task done.
  </action>
  <verify>
    <automated>cd "/Users/gerardvinou/Claude code/Claude Projects/project-template-v2" && F=".claude/skills/sparc/SKILL.md" && PASS=0 && FAIL=0 && for pat in "## Phase 1" "## Phase 2" "## Phase 3" "## Phase 4" "## Phase 5" "architect.*tier 3" "critic.*tier 3" "reviewer.*routing\|reviewer.*swarm" "critic.*routing\|critic.*swarm" "Wait for confirmation before Phase 2" "Wait for confirmation before Phase 3" "Wait for confirmation before Phase 4" "Wait for confirmation before Phase 5" "GSD standard execution" "sparc:review" "sparc-spec.md" "sparc-pseudo.md" "sparc-arch.md" "sparc-review.md" "docs/architecture/contexts.md"; do if grep -qE "$pat" "$F" 2>/dev/null; then echo "PASS: $pat"; PASS=$((PASS+1)); else echo "FAIL: $pat"; FAIL=$((FAIL+1)); fi; done && echo "--- Results: $PASS passed, $FAIL failed ---" && LINES=$(wc -l < "$F") && if [ "$LINES" -lt 150 ]; then echo "PASS: line count $LINES < 150"; else echo "FAIL: line count $LINES >= 150"; fi</automated>
  </verify>
  <acceptance_criteria>
    - All 20 grep patterns return PASS (0 failures)
    - Line count is under 150
    - SPARC-01 through SPARC-09 all confirmed satisfied
    - No regression in Phase 1-4 content (spec-writer, logic-planner, architect, critic Phase 3, GSD delegation)
  </acceptance_criteria>
  <done>
    All 9 SPARC requirements validated as satisfied. SPARC SKILL.md is ready for downstream consumption by Phase 5 (DDD contexts.md wiring) and Phase 7 (template references).
  </done>
</task>

</tasks>

<verification>
All 9 SPARC requirements are satisfied in `.claude/skills/sparc/SKILL.md`:
- SPARC-01: 5 phases present (Spec, Pseudo, Arch, Refine, Complete)
- SPARC-02: Phase 3 dual-agent architect + critic in parallel at tier 3
- SPARC-03: Phase 5 dual-agent reviewer + critic per swarm-patterns.md routing
- SPARC-04: All phase transitions require user validation (no exceptions)
- SPARC-05: Phase 4 delegates to GSD standard execution
- SPARC-06: Standalone `/sparc:review` entry point for Phase 5 alone
- SPARC-07: Workspace files at `.claude/workspace/sparc-{spec,pseudo,arch,review}.md`
- SPARC-08: File under 150 lines
- SPARC-09: `docs/architecture/contexts.md` passed as constraint in Phase 3
</verification>

<success_criteria>
- 3 targeted edits applied to SKILL.md (Phase 5 dual-agent, validation gate, standalone entry)
- 20+ grep validation patterns pass across all 9 requirements
- No content regressions (Phases 1-4, skip rules, workspace paths, "What SPARC Does NOT Do")
- SKILL.md stays under 150 lines
</success_criteria>

<output>
After completion, create `.planning/phases/03-sparc-skill/03-01-SUMMARY.md`
</output>
