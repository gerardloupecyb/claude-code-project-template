---
phase: 02-preflight-agent5-critic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/pre-flight/SKILL.md
autonomous: true
requirements: [PREFLT-01, PREFLT-02, PREFLT-03, PREFLT-04, PREFLT-05, PREFLT-06]

must_haves:
  truths:
    - "Agent 5 Critic appears in pre-flight SKILL.md and runs sequentially after Agent 1"
    - "Agent 5 references the critic role from swarm-patterns.md for model selection, not a hardcoded tier"
    - "Pre-flight report template includes an Architecture Challenge section with challenge/resolution format"
    - "Agent 5 is skipped when Agent 1 returns no architectural findings"
    - "Agent 5 reads docs/architecture/contexts.md as a constraint when the file exists and has real content"
    - "Report synthesis references all 5 agents, not just 4"
  artifacts:
    - path: ".claude/skills/pre-flight/SKILL.md"
      provides: "Pre-flight multi-agent plan review with Agent 5 Critic"
      contains: "role: critic (ref: .claude/rules/swarm-patterns.md)"
  key_links:
    - from: ".claude/skills/pre-flight/SKILL.md"
      to: ".claude/rules/swarm-patterns.md"
      via: "Agent 5 critic role reference and model routing delegation"
      pattern: "ref: \\.claude/rules/swarm-patterns\\.md"
    - from: ".claude/skills/pre-flight/SKILL.md"
      to: "docs/architecture/contexts.md"
      via: "Agent 5 bounded context constraint check"
      pattern: "docs/architecture/contexts\\.md"
---

<objective>
Close 3 targeted gaps in the pre-flight SKILL.md so that Agent 5 Critic fully satisfies all PREFLT requirements.

The pre-flight SKILL.md was built during Track A integration and already contains Agent 5 with most required behavior. However, gap analysis against PREFLT-01 through PREFLT-06 reveals 3 specific issues:

1. **PREFLT-03 gap**: Line 105 hardcodes `model: Opus (tier 3)` which contradicts the Phase 1 decision that "critic defaults to Sonnet; escalates to Opus per escalation triggers." The model line should delegate to swarm-patterns.md routing.
2. **PREFLT-06 gap**: Line 118 checks for contexts.md but doesn't specify behavior when file contains only unfilled placeholders (should skip gracefully).
3. **Report consistency**: Line 124 says "After all 4 agents complete" but should say "After all 5 agents complete" since Agent 5 is now part of the synthesis flow.

Requirements already satisfied (no changes needed):
- PREFLT-01: Agent 5 present at lines 101-118
- PREFLT-02: Sequential execution documented at lines 101, 108
- PREFLT-04: Architecture Challenge section at lines 157-159
- PREFLT-05: Trivial-plan skip at line 109

Purpose: Ensure pre-flight Critic agent fully aligns with Phase 1 swarm-patterns decisions and handles edge cases.
Output: Updated `.claude/skills/pre-flight/SKILL.md` with 3 targeted line edits.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-swarm-patterns/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key content the executor needs from swarm-patterns.md (Phase 1 output) -->

From .claude/rules/swarm-patterns.md (lines 8-10):
```
| `critic` | Challenge, edge cases, over-engineering | Sonnet/Opus | Cherche activement les problemes |
```

Line 18:
```
critic defaults to Sonnet; escalates to Opus per escalation triggers below.
```

Escalation triggers (lines 55-60):
```
**Escalate to Opus when:**
- Auth, security, permissions, or sensitive data
- Bounded contexts or API boundaries in scope
- Architect and critic contradict without clear resolution
- 2nd NO-GO on the same task
- Costly-to-reverse decision (schema, API contract, data model, orchestration)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix 3 targeted gaps in pre-flight SKILL.md</name>
  <files>.claude/skills/pre-flight/SKILL.md</files>
  <read_first>
    - .claude/skills/pre-flight/SKILL.md (the file being modified -- read in full)
    - .claude/rules/swarm-patterns.md (source of truth for critic model routing)
  </read_first>
  <action>
Make exactly 3 edits to `.claude/skills/pre-flight/SKILL.md`:

**Edit 1 — Fix model routing (PREFLT-03, line 105):**
Replace the current Agent 5 code block:
```
role: critic (ref: .claude/rules/swarm-patterns.md)
model: Opus (tier 3)
```
With:
```
role: critic (ref: .claude/rules/swarm-patterns.md)
model: per swarm-patterns.md routing (Sonnet default; Opus on escalation triggers)
```
This aligns with the Phase 1 decision that "critic defaults to Sonnet; escalates to Opus per escalation triggers" (see 01-01-SUMMARY.md key-decisions). The hardcoded Opus tier contradicts this decision.

**Edit 2 — Add placeholder-skip to contexts.md check (PREFLT-06, line 118):**
Replace the current line:
```
If `docs/architecture/contexts.md` exists: verify the plan respects bounded contexts.
```
With:
```
If `docs/architecture/contexts.md` exists and contains real content (not just unfilled
placeholders): verify the plan respects bounded contexts. Skip if file is absent or
contains only template placeholders.
```
This ensures Agent 5 does not fail or produce false findings when contexts.md exists but has only `{{placeholder}}` content.

**Edit 3 — Fix agent count in report synthesis (report consistency, line 124):**
Replace:
```
After all 4 agents complete, synthesize their findings into a structured report.
```
With:
```
After all 5 agents complete, synthesize their findings into a structured report.
```
This reflects that Agent 5 (Critic) is now part of the flow and its output feeds into the Architecture Challenge section of the report.

**Do NOT change anything else.** All other content (Agent 1-4 definitions, report template sections, verdict rules, Codex cross-model challenge, etc.) is correct and must remain as-is.
  </action>
  <verify>
    <automated>cd "/Users/gerardvinou/Claude code/Claude Projects/project-template-v2" && echo "=== PREFLT-03: model routing ===" && grep -n "per swarm-patterns.md routing" .claude/skills/pre-flight/SKILL.md && echo "=== PREFLT-06: placeholder skip ===" && grep -n "template placeholders" .claude/skills/pre-flight/SKILL.md && echo "=== Report agent count ===" && grep -n "After all 5 agents" .claude/skills/pre-flight/SKILL.md && echo "=== No hardcoded Opus in Agent 5 ===" && grep -c "model: Opus" .claude/skills/pre-flight/SKILL.md | grep -q "^0$" && echo "PASS: no hardcoded Opus" || echo "FAIL: still has hardcoded Opus"</automated>
  </verify>
  <acceptance_criteria>
    - grep "per swarm-patterns.md routing" SKILL.md returns exactly 1 match (Edit 1 applied)
    - grep "model: Opus" SKILL.md returns 0 matches (hardcoded Opus removed)
    - grep "template placeholders" SKILL.md returns exactly 1 match (Edit 2 applied)
    - grep "After all 5 agents" SKILL.md returns exactly 1 match (Edit 3 applied)
    - grep "After all 4 agents" SKILL.md returns 0 matches (old count removed)
    - Total line count of SKILL.md stays under 240 lines (currently 236)
    - All other content unchanged (Agent 1-4, report template structure, verdict rules, Codex section)
  </acceptance_criteria>
  <done>
    Three targeted edits applied: (1) Agent 5 model routing delegates to swarm-patterns.md instead of hardcoding Opus, (2) contexts.md check includes placeholder-skip instruction, (3) report synthesis references all 5 agents.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate all 6 PREFLT requirements are satisfied</name>
  <files>.claude/skills/pre-flight/SKILL.md</files>
  <read_first>
    - .claude/skills/pre-flight/SKILL.md (re-read after Task 1 edits)
    - .planning/REQUIREMENTS.md (requirements to validate against)
  </read_first>
  <action>
Run grep-based validation for each PREFLT requirement against the updated SKILL.md. This is a read-only validation task -- no file edits.

Check each requirement:

**PREFLT-01** — Agent 5 Critic added in SKILL.md:
```
grep "Agent 5: Architecture Critic" SKILL.md
```
Expected: match on the Agent 5 heading.

**PREFLT-02** — Agent 5 sequential, receives Agent 1 output:
```
grep "sequential.*waits for Agent 1" SKILL.md
grep "Receives: Agent 1" SKILL.md
```
Expected: both match.

**PREFLT-03** — References critic role from swarm-patterns.md:
```
grep "role: critic (ref: .claude/rules/swarm-patterns.md)" SKILL.md
grep "per swarm-patterns.md routing" SKILL.md
```
Expected: both match. No "model: Opus" hardcoded.

**PREFLT-04** — Architecture Challenge section in report:
```
grep "### Architecture Challenge" SKILL.md
```
Expected: match.

**PREFLT-05** — Skip when trivial:
```
grep "Skip if plan is trivial" SKILL.md
```
Expected: match.

**PREFLT-06** — Reads contexts.md as constraint when file exists:
```
grep "docs/architecture/contexts.md" SKILL.md
grep "template placeholders" SKILL.md
```
Expected: both match (contexts.md reference + placeholder-skip).

Report pass/fail for each requirement. If any fail, diagnose and fix before marking task done.
  </action>
  <verify>
    <automated>cd "/Users/gerardvinou/Claude code/Claude Projects/project-template-v2" && F=".claude/skills/pre-flight/SKILL.md" && PASS=0 && FAIL=0 && for pat in "Agent 5: Architecture Critic" "sequential.*waits for Agent 1" "Receives: Agent 1" "role: critic (ref:" "per swarm-patterns.md routing" "### Architecture Challenge" "Skip if plan is trivial" "docs/architecture/contexts.md" "template placeholders"; do if grep -q "$pat" "$F" 2>/dev/null; then echo "PASS: $pat"; PASS=$((PASS+1)); else echo "FAIL: $pat"; FAIL=$((FAIL+1)); fi; done && echo "--- Results: $PASS passed, $FAIL failed ---"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 grep patterns return PASS (0 failures)
    - PREFLT-01 through PREFLT-06 all confirmed satisfied
    - No regression in existing content (Agent 1-4, report template, verdict rules)
  </acceptance_criteria>
  <done>
    All 6 PREFLT requirements validated as satisfied. Pre-flight SKILL.md is ready for downstream consumption by Phase 3 (SPARC) and Phase 5 (DDD contexts.md wiring).
  </done>
</task>

</tasks>

<verification>
All 6 PREFLT requirements are satisfied in `.claude/skills/pre-flight/SKILL.md`:
- PREFLT-01: Agent 5 Critic section present (lines ~101-118)
- PREFLT-02: Sequential execution after Agent 1 documented
- PREFLT-03: Critic role references swarm-patterns.md, model routing delegated (not hardcoded)
- PREFLT-04: Architecture Challenge section in report template
- PREFLT-05: Trivial-plan skip condition present
- PREFLT-06: contexts.md constraint check with placeholder-skip
</verification>

<success_criteria>
- 3 targeted edits applied to SKILL.md (model routing, placeholder-skip, agent count)
- 9/9 grep validation patterns pass
- No content regressions (Agent 1-4, report template, verdict rules, Codex section unchanged)
- SKILL.md stays under 240 lines
</success_criteria>

<output>
After completion, create `.planning/phases/02-preflight-agent5-critic/02-01-SUMMARY.md`
</output>
