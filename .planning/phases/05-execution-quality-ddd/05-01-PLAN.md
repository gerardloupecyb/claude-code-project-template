---
phase: 05-execution-quality-ddd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/rules/execution-quality.md
autonomous: true
requirements:
  - ADR-01
  - ADR-02
  - ADR-03
  - DDD-01
  - DDD-02
  - DDD-03
  - DDD-04
  - DDD-05
must_haves:
  truths:
    - "execution-quality.md contains a Decision Tracking section with skip heuristic (ADR-01, ADR-02 — pre-satisfied)"
    - "execution-quality.md total line count is strictly under 80 lines (ADR-03)"
    - "docs/architecture/contexts.md.template exists with domain placeholder sections (DDD-01 — pre-satisfied)"
    - "Pre-flight SKILL.md Agent 5 references contexts.md (DDD-02 — pre-satisfied)"
    - "SPARC SKILL.md Phase 3 references contexts.md (DDD-03 — pre-satisfied)"
    - "Reference Layer Awareness table includes a cross-domain row pointing to contexts.md (DDD-04)"
    - "Agents ignore unfilled placeholder contexts.md (DDD-05 — pre-satisfied)"
  artifacts:
    - path: ".claude/rules/execution-quality.md"
      provides: "Execution quality rules with DDD-04 contexts.md check and ADR-03 line budget"
      contains: "contexts.md"
      max_lines: 79
  key_links:
    - from: ".claude/rules/execution-quality.md"
      to: "docs/architecture/contexts.md"
      via: "Reference Layer Awareness table row"
      pattern: "Cross-domain.*contexts\\.md"
---

<objective>
Close the 2 remaining gaps in Phase 5 by editing execution-quality.md: add a contexts.md
check row to the Reference Layer Awareness table (DDD-04) and compress the file from ~102
lines to under 80 (ADR-03). All other Phase 5 requirements (ADR-01, ADR-02, DDD-01,
DDD-02, DDD-03, DDD-05) are already satisfied from Track A and must not be regressed.

Purpose: Cross-domain boundaries become visible in the execution quality checklist, and the
file stays within its 80-line budget so .claude/rules/ auto-loading remains lightweight.

Output: Updated `.claude/rules/execution-quality.md`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-execution-quality-ddd/05-CONTEXT.md
@.claude/rules/execution-quality.md
</context>

<interfaces>
<!-- Current structure of execution-quality.md (100 lines) -->
<!-- Lines 33-47: Reference Layer Awareness — DDD-04 row goes here -->
<!-- Lines 89-100: Reviewer Agents — primary compression target -->

Current Reference Layer Awareness table (lines 37-42):
```
| Task type | Reference to read | Path |
|-----------|------------------|------|
| Infra change (Docker, secrets, deploy, MCP) | L3 Services | `docs/references/services-and-access.md` |
| Shared module or schema change | L3 Codebase | `docs/references/codebase-context.md` |
| Architecture or security decision | L1 Architecture | `docs/references/architecture-security.md` |
| Writing code in project languages | L2 Patterns | `docs/references/coding-patterns.md` |
```

Current Reviewer Agents section (lines 89-100, 12 lines — most compressible):
```markdown
## Reviewer Agents (Post-Phase, Optional)

After completing all tasks in a phase, consider launching reviewer agents for complex or risky changes:

| Change type | Reviewer agent |
|-------------|---------------|
| Security-sensitive (auth, payments, secrets) | `security-sentinel` |
| Database migrations or data transforms | `data-integrity-guardian` |
| Architecture changes (new services, API contracts) | `architecture-strategist` |
| Performance-critical paths | `performance-oracle` |

**Skip heuristic:** Skip if changes are leaf-node, < 50 LOC, or purely additive (new file, no existing code modified). Only invoke when the change could break things you can't see from the diff alone.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add DDD-04 contexts.md row and compress to under 80 lines</name>
  <files>.claude/rules/execution-quality.md</files>
  <read_first>
    - .claude/rules/execution-quality.md (full — this is the file being edited)
    - .planning/phases/05-execution-quality-ddd/05-CONTEXT.md (decisions D-07 through D-13)
  </read_first>
  <action>
Edit `.claude/rules/execution-quality.md` in two parts:

**Part A — DDD-04: Add contexts.md row (per D-11, D-12, D-13)**

In the Reference Layer Awareness table (after the last row "Writing code in project languages"), add one new row:

```
| Cross-domain change | DDD Contexts | `docs/architecture/contexts.md` |
```

Update the skip heuristic on line 47 to also cover contexts.md placeholders (per D-13). Append to the existing skip heuristic sentence: ". For `contexts.md`, also skip if file contains only `{{` placeholders."

**Part B — ADR-03: Compress to under 80 lines (per D-07, D-08, D-09, D-10)**

After Part A the file will be ~102 lines. Need to cut at least 23 lines. Compression strategy:

1. **Reviewer Agents section (target: save 7-8 lines):** Collapse the intro paragraph into the section header. Remove the blank line before the table. Merge the skip heuristic into one tight line. Target: 5 lines total (header + table rows + skip).

2. **Post-Deploy Monitoring section (target: save 3-4 lines):** Convert the 4-bullet list into a single-line format: "Document: logs/search terms, metrics/dashboards, healthy signal, failure signal + rollback trigger." Keep the "no runtime impact" line.

3. **System-Wide Test Check section (target: save 3-4 lines):** Remove the "Before marking a code task done, answer these 5 questions:" intro line (the table is self-explanatory). Remove the blank line before "Skip heuristic."

4. **Simplify As You Go section (target: save 3-4 lines):** Merge the 3 numbered items into one line: "Consolidate duplicates, extract shared logic, remove dead code." Merge the two paragraphs (skip heuristic + timing) into one.

5. **Decision Tracking section (target: save 2-3 lines):** Collapse the 2-line intro paragraph into one. Keep the table and skip lines intact.

6. **Commit Quality Heuristics (target: save 1-2 lines):** Remove the blank line before "Heuristic:".

7. **Header/meta (target: save 1 line):** Merge APPLICABILITY and PRECEDENCE onto fewer lines if possible.

**Constraints (per D-09, D-10):**
- Keep ALL section headers exactly as-is (they are referenced by other files)
- Keep ALL tables intact (compress prose around them, not table content)
- Do NOT extract sections to separate files
- Do NOT remove any content section — only compress prose

**Final target: 75-79 lines (strictly under 80).**
  </action>
  <verify>
    <automated>wc -l .claude/rules/execution-quality.md | awk '{if ($1 < 80) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (must be < 80)"}'</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Cross-domain.*contexts.md" .claude/rules/execution-quality.md returns 1 (DDD-04 row present)
    - grep -c "placeholder" .claude/rules/execution-quality.md returns at least 1 (DDD-04 skip heuristic for placeholders)
    - wc -l .claude/rules/execution-quality.md shows strictly less than 80 (ADR-03)
    - grep -c "Decision Tracking" .claude/rules/execution-quality.md returns 1 (ADR-01 preserved)
    - grep -c "trivial decisions" .claude/rules/execution-quality.md returns 1 (ADR-02 skip heuristic preserved)
    - grep -c "Reviewer Agents" .claude/rules/execution-quality.md returns 1 (section preserved)
    - grep -c "Reference Layer Awareness" .claude/rules/execution-quality.md returns 1 (section preserved)
    - All 7 section headers still present: System-Wide Test Check, Post-Deploy Monitoring, Reference Layer Awareness, Commit Quality Heuristics, Decision Tracking, Simplify As You Go, Reviewer Agents
  </acceptance_criteria>
  <done>
    execution-quality.md has the contexts.md row in Reference Layer Awareness table,
    placeholder skip heuristic is documented, file is under 80 lines, and all existing
    content sections are preserved (compressed, not removed).
  </done>
</task>

</tasks>

<verification>
All 8 requirement IDs verified:

| ID | Check | Method |
|----|-------|--------|
| ADR-01 | Decision Tracking section exists | `grep "Decision Tracking" .claude/rules/execution-quality.md` |
| ADR-02 | Skip heuristic for trivial decisions | `grep "trivial decisions" .claude/rules/execution-quality.md` |
| ADR-03 | File under 80 lines | `wc -l .claude/rules/execution-quality.md` < 80 |
| DDD-01 | contexts.md.template exists | `test -f docs/architecture/contexts.md.template` (pre-satisfied) |
| DDD-02 | Pre-flight references contexts.md | `grep "contexts.md" .claude/skills/pre-flight/SKILL.md` (pre-satisfied) |
| DDD-03 | SPARC references contexts.md | `grep "contexts.md" .claude/skills/sparc/SKILL.md` (pre-satisfied) |
| DDD-04 | contexts.md row in Reference Layer table | `grep "Cross-domain.*contexts.md" .claude/rules/execution-quality.md` |
| DDD-05 | Agents ignore unfilled placeholders | `grep "placeholder" .claude/skills/pre-flight/SKILL.md` (pre-satisfied) |
</verification>

<success_criteria>
- execution-quality.md contains "Cross-domain" row pointing to `docs/architecture/contexts.md`
- execution-quality.md is strictly under 80 lines
- All 7 section headers preserved
- No content sections removed (only prose compressed)
- No other files modified (Phases 1-4 outputs are locked)
</success_criteria>

<output>
After completion, create `.planning/phases/05-execution-quality-ddd/05-01-SUMMARY.md`
</output>
