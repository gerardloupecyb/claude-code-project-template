---
phase: 07-template-init
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - CLAUDE.md.template
autonomous: true
requirements: [TMPL-01, TMPL-02, TMPL-03, TMPL-04, TMPL-05, TMPL-06, TMPL-07]
must_haves:
  truths:
    - "CLAUDE.md.template is under 200 lines"
    - "CLAUDE.md.template references SPARC, swarm-patterns, and AgentDB"
    - "CLAUDE.md.template documents the SPARC workflow path"
    - "Closure protocol is present but compressed to ~10 lines"
    - "Session start/end contract (Rules 1-2) is present but compressed to ~10 lines"
    - "Pointers to all rules/ files are explicit"
    - "Active domains table is preserved"
  artifacts:
    - path: "CLAUDE.md.template"
      provides: "Compressed project identity + entry points + contracts with pointers to rules/"
      min_lines: 70
  key_links:
    - from: "CLAUDE.md.template"
      to: ".claude/rules/flywheel-workflow.md"
      via: "explicit pointer"
      pattern: "flywheel-workflow.md"
    - from: "CLAUDE.md.template"
      to: ".claude/rules/tool-routing.md"
      via: "explicit pointer"
      pattern: "tool-routing.md"
    - from: "CLAUDE.md.template"
      to: ".claude/rules/coordination.md"
      via: "explicit pointer"
      pattern: "coordination.md"
    - from: "CLAUDE.md.template"
      to: ".claude/rules/planning-discipline.md"
      via: "explicit pointer"
      pattern: "planning-discipline.md"
---

<objective>
Compress CLAUDE.md.template from 505 lines to under 200 lines by removing all content that was extracted to rules/ files in Plan 01, trimming retained sections, and adding explicit pointers.

Purpose: TMPL-03 requires < 200 lines. The template must remain the single entry point that orients Claude, but detailed procedures now live in auto-loaded rules/ files.

Output: CLAUDE.md.template at 80-100 lines with all 7 TMPL requirements satisfied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-template-init/07-CONTEXT.md
@.planning/phases/07-template-init/07-01-SUMMARY.md

Source file:
@CLAUDE.md.template

Pointers needed to (verify these exist after Plan 01):
@.claude/rules/flywheel-workflow.md
@.claude/rules/tool-routing.md
@.claude/rules/coordination.md
@.claude/rules/planning-discipline.md
@.claude/rules/execution-quality.md
@.claude/rules/workflow-automation.md
@.claude/rules/swarm-patterns.md
@.claude/rules/model-routing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md.template to compressed form</name>
  <files>CLAUDE.md.template</files>
  <read_first>
    - CLAUDE.md.template (full — understand current structure before rewriting)
    - .planning/phases/07-template-init/07-01-SUMMARY.md (confirm extraction is done)
    - .planning/phases/07-template-init/07-CONTEXT.md (re-read D-08 through D-14 for what stays)
  </read_first>
  <action>
Rewrite CLAUDE.md.template as a complete file replacement. The new file must follow this exact structure, implementing decisions D-08 through D-14:

**Section 1: Project Identity (D-08) — ~30 lines**
Keep lines 1-32 of the original EXACTLY as-is (stack, outils actifs, MCP actifs, skills installes). These are project-specific placeholders that orient Claude.

**Section 2: Memory Files Table (D-09) — ~20 lines**
Keep the table from lines 37-48 exactly. Remove lines 49-77 (the "/memory-consolidate" prose, the "Separation des fichiers" prose). Replace with a 2-line summary:

```markdown
Pour la cohérence mémoire, utiliser `/memory-consolidate quick`.
Séparation : STATE.md = GPS technique, MEMORY.md = journal de bord, LESSONS.md = cache chaud.
```

**Section 3: Workflows — 3 entry points (D-10) — ~25 lines**
Keep the 3 workflow paths (lines 82-106) as short summaries. Remove the "Chainage brainstorm" section, the "Workflow detaille par phase" section (lines 136-158) — these are detailed procedures covered by prepare-phase skill and workflow-automation.md.

Keep only:
```markdown
## Workflows — choisir le bon parcours

### Phase GSD
/prepare-phase {N} → /sparc "description" → closure → /lesson → /gsd:verify-work → /ce:review

### Tache isolee (hors GSD)
/ce:brainstorm → /ce:plan → /ce:work → closure → /lesson → /ce:review → /ce:compound

### Grosse feature (multi-phases)
/ce:brainstorm → /ce:plan → /prepare-phase {N} → /sparc → closure → /gsd:verify-work → /ce:review → /ce:compound

Brainstorms < 7 jours : lire `docs/brainstorms/` avant planification.
```

**Section 4: Closure Protocol (D-11) — ~10 lines**
Compress the closure protocol (lines 114-135) to essentials:

```markdown
### Protocole closure (obligatoire apres chaque execution)

1. Ecrire SUMMARY.md : planifie vs realise, decisions, items differes
2. `/memory-consolidate quick` pour detecter incoherences
3. Mettre a jour MEMORY.md (Regle #2)
4. Proposer `/lesson` si fix non-trivial
5. Lancer tests existants — corriger AVANT /gsd:verify-work
6. Quality score dans SUMMARY.md : AC coverage, deviations, verdict CLEAN|ROUGH
7. Si decisions prises → proposer ajout DECISIONS.md (format DEC-NNN)
```

**Section 5: Session Contract — Rules 1-2 (D-12) — ~15 lines**
Compress Rules 1-2 (lines 193-231) to contract form:

```markdown
## Gestion du contexte

### Regle 1 — Debut de session
Hook SessionStart injecte MEMORY.md + LESSONS.md automatiquement.
CARL charge le domaine selon contexte. Ne pas charger AgentDB au demarrage.
Ne jamais coder avant que MEMORY.md et LESSONS.md soient en contexte.
Vider "Deviations d'execution" de MEMORY.md (donnees session precedente).

### Regle 2 — Fin de session
Mettre a jour MEMORY.md : ce qui a ete fait (3-5 lignes), decisions, prochaine etape, blocages.
Cap : 5 dernieres entrees dans "Ce qui a ete fait" (historique dans Git).
Si fix non-trivial → `/lesson`. Si pattern lourd → `/ce:compound`.
Commiter MEMORY.md + LESSONS.md avec le code.
```

**Section 6: Lesson Capture (trimmed) — ~8 lines**
Compress lines 398-414 (lesson capture two paths):

```markdown
## Capture des lecons

**Chemin rapide** — `/lesson` : format quand/faire/parce que → LESSONS.md. Si critique → promotion CARL. Si cap 50 → `/lesson migrate` vers AgentDB + docs/solutions/.
**Chemin complet** — `/ce:compound` : patterns lourds → docs/solutions/. Voir `.claude/rules/flywheel-workflow.md`.
```

**Section 7: Pointers to Rules (D-14) — ~15 lines**
Add explicit pointers to ALL rules/ files for everything extracted:

```markdown
## Regles detaillees — fichiers auto-charges

Les regles suivantes sont dans `.claude/rules/` et chargees automatiquement :

| Regle | Fichier | Contenu |
|-------|---------|---------|
| Outils + COT (R3-R4) | `tool-routing.md` | Routing outils, checkpoints contexte, chain of thought |
| Flywheel + AgentDB + CARL (R5-R7) | `flywheel-workflow.md` | Flywheel, AgentDB format, CARL enrichissement, anti-patterns |
| Plans + metadata (R8) | `planning-discipline.md` | AC format BDD, frontieres, metadata docs/, classification, consultation |
| Coordination GSD-CE | `coordination.md` | Table fichiers partages, lecture croisee GSD/Compound |
| Qualite execution | `execution-quality.md` | Test checks, monitoring, reference layer, decisions, simplification |
| Transitions | `workflow-automation.md` | Auto-detection, choix executor, transitions post-execution |
| Multi-agent | `swarm-patterns.md` | 8 roles, topologies, model routing 3-tier, SPARC routing |
| Delegation modeles | `model-routing.md` | Classification binaire, handoff, brief structure |
| Todos | `todo-discipline.md` | Routing /todo, ne jamais ecrire directement dans todos/ |
```

**Section 8: Active Domains Table (D-13) — ~6 lines**
Keep lines 500-505 exactly as-is.

**TOTAL TARGET: ~80-100 lines (well under 200)**

**REMOVED (content now in rules/ files):**
- Lines 49-77: Memory separation prose → covered by 2-line summary
- Lines 108-113: Brainstorm chaining detail → 1-line summary
- Lines 136-189: Workflow detail + distinction + metadata + classification → planning-discipline.md
- Lines 234-276: Rules 3-4 → tool-routing.md
- Lines 278-355: Rules 5-7 → flywheel-workflow.md
- Lines 358-395: Rule 8 → planning-discipline.md
- Lines 418-444: Consultation + reference → planning-discipline.md + execution-quality.md
- Lines 447-498: Coordination + anti-patterns → coordination.md + flywheel-workflow.md
  </action>
  <verify>
    <automated>
cd "/Users/gerardvinou/Claude code/Claude Projects/project-template-v2" && \
LINES=$(wc -l < CLAUDE.md.template) && \
echo "Line count: $LINES" && \
test "$LINES" -lt 200 && echo "TMPL-03 PASS: under 200 lines" || echo "TMPL-03 FAIL: $LINES >= 200" && \
echo "--- TMPL-01 refs ---" && \
grep -c "SPARC\|sparc" CLAUDE.md.template && \
grep -c "swarm-patterns" CLAUDE.md.template && \
grep -c "AgentDB" CLAUDE.md.template && \
echo "--- TMPL-02 SPARC flow ---" && \
grep -c "/sparc" CLAUDE.md.template && \
echo "--- D-11 Closure ---" && \
grep -c "closure\|SUMMARY" CLAUDE.md.template && \
echo "--- D-12 Rules 1-2 ---" && \
grep -c "Regle.*1\|Regle.*2\|Début de session\|Fin de session\|Debut de session" CLAUDE.md.template && \
echo "--- D-14 Pointers ---" && \
grep -c "tool-routing.md" CLAUDE.md.template && \
grep -c "flywheel-workflow.md" CLAUDE.md.template && \
grep -c "coordination.md" CLAUDE.md.template && \
grep -c "planning-discipline.md" CLAUDE.md.template && \
grep -c "execution-quality.md" CLAUDE.md.template && \
echo "--- D-13 Domains ---" && \
grep -c "DOMAIN_1\|DOMAIN_2" CLAUDE.md.template && \
echo "VERIFICATION COMPLETE"
    </automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md.template < 200 lines (TMPL-03)
    - Contains references to SPARC, swarm-patterns, AgentDB (TMPL-01)
    - Contains /sparc workflow path (TMPL-02)
    - Closure protocol present but ~10 lines (D-11)
    - Rules 1-2 present but ~15 lines total (D-12)
    - Explicit pointer table to all 9 rules/ files (D-14)
    - Active domains table preserved (D-13)
    - No {{PLACEHOLDER}} values lost from sections that were kept
    - Lines 234-498 content is fully absent (extracted in Plan 01)
  </acceptance_criteria>
  <done>CLAUDE.md.template is 80-100 lines, under 200 (TMPL-03), with all identity/workflow/contract content preserved in compressed form and explicit pointers to rules/ files for all extracted detail.</done>
</task>

<task type="auto">
  <name>Task 2: Final verification of all 7 TMPL requirements</name>
  <files></files>
  <read_first>
    - CLAUDE.md.template (verify final state)
    - init-project.sh (verify copies all artifacts)
  </read_first>
  <action>
Run a comprehensive verification of all 7 TMPL requirements:

**TMPL-01** (SPARC, swarm, AgentDB refs): grep CLAUDE.md.template for "SPARC", "swarm-patterns", "AgentDB" — all must appear.

**TMPL-02** (SPARC flow documented): grep for "/sparc" in a workflow path — must show the prepare-phase → sparc → closure flow.

**TMPL-03** (< 200 lines): `wc -l CLAUDE.md.template` must be < 200.

**TMPL-04** (init copies contexts.md.template): grep init-project.sh for "contexts.md" — must find the cp command.

**TMPL-05** (init copies pre-agent.sh): grep init-project.sh for "pre-agent.sh" — must find the cp command.

**TMPL-06** (init wires settings.json): grep init-project.sh for "settings.json" — must find the cp command.

**TMPL-07** (init .claude/workspace/ to gitignore): grep init-project.sh for "workspace" — must find the reference. Also verify .gitignore template contains the entry.

If ANY requirement fails, fix it immediately. Report the final status of all 7.
  </action>
  <verify>
    <automated>
cd "/Users/gerardvinou/Claude code/Claude Projects/project-template-v2" && \
echo "=== TMPL-01 ===" && \
grep -q "SPARC\|sparc" CLAUDE.md.template && echo "SPARC: PASS" || echo "SPARC: FAIL" && \
grep -q "swarm-patterns" CLAUDE.md.template && echo "swarm: PASS" || echo "swarm: FAIL" && \
grep -q "AgentDB" CLAUDE.md.template && echo "AgentDB: PASS" || echo "AgentDB: FAIL" && \
echo "=== TMPL-02 ===" && \
grep -q "/sparc" CLAUDE.md.template && echo "SPARC flow: PASS" || echo "SPARC flow: FAIL" && \
echo "=== TMPL-03 ===" && \
LINES=$(wc -l < CLAUDE.md.template) && \
test "$LINES" -lt 200 && echo "Lines ($LINES): PASS" || echo "Lines ($LINES): FAIL" && \
echo "=== TMPL-04 ===" && \
grep -q "contexts.md" init-project.sh && echo "contexts.md: PASS" || echo "contexts.md: FAIL" && \
echo "=== TMPL-05 ===" && \
grep -q "pre-agent.sh" init-project.sh && echo "pre-agent.sh: PASS" || echo "pre-agent.sh: FAIL" && \
echo "=== TMPL-06 ===" && \
grep -q "settings.json" init-project.sh && echo "settings.json: PASS" || echo "settings.json: FAIL" && \
echo "=== TMPL-07 ===" && \
grep -q "workspace" init-project.sh && echo "workspace: PASS" || echo "workspace: FAIL" && \
echo "=== ALL REQUIREMENTS CHECKED ==="
    </automated>
  </verify>
  <acceptance_criteria>
    - All 7 TMPL requirements pass verification
    - No regression in init-project.sh functionality
    - CLAUDE.md.template has no orphaned {{PLACEHOLDER}} references that were in removed sections
  </acceptance_criteria>
  <done>All 7 TMPL requirements verified as PASS. Phase 07 goal achieved: new projects get all Milestone 1 enrichments wired automatically, CLAUDE.md.template is under 200 lines.</done>
</task>

</tasks>

<verification>
Phase 07 complete when:
1. CLAUDE.md.template < 200 lines (from 505)
2. All extracted content lives in rules/ files (Plan 01)
3. init-project.sh copies all new rule files to new projects
4. All 7 TMPL requirements pass grep-based verification
5. No information loss — every procedural block has a new home
</verification>

<success_criteria>
- CLAUDE.md.template: 80-100 lines, under 200 (TMPL-03)
- TMPL-01: References SPARC, swarm-patterns, AgentDB
- TMPL-02: Documents SPARC workflow path
- TMPL-04/05/06/07: init-project.sh copies contexts.md, pre-agent.sh, settings.json, workspace gitignore (pre-satisfied, verified)
- Zero content loss from extraction
</success_criteria>

<output>
After completion, create `.planning/phases/07-template-init/07-02-SUMMARY.md`
</output>
