---
phase: 07-template-init
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/rules/flywheel-workflow.md
  - .claude/rules/tool-routing.md
  - .claude/rules/coordination.md
  - .claude/rules/planning-discipline.md
autonomous: true
requirements: [TMPL-03]
must_haves:
  truths:
    - "Rules 5-7 content (AgentDB format, CARL enrichment, anti-patterns) exists in flywheel-workflow.md"
    - "Rules 3-4 content (COT trigger matrix, context checkpoints) exists in tool-routing.md"
    - "GSD-Compound coordination table exists in coordination.md"
    - "Plan structure rules and frontmatter conventions exist in planning-discipline.md"
    - "No content is lost -- every extracted block has a new home in a rules/ file"
  artifacts:
    - path: ".claude/rules/flywheel-workflow.md"
      provides: "Merged flywheel + Rules 5-7 (AgentDB, CARL, anti-patterns)"
      contains: "AgentDB"
    - path: ".claude/rules/tool-routing.md"
      provides: "Merged tool routing + COT trigger matrix + context checkpoints"
      contains: "Chain of thought"
    - path: ".claude/rules/coordination.md"
      provides: "GSD-Compound coordination table and shared files"
      contains: "Compound"
    - path: ".claude/rules/planning-discipline.md"
      provides: "Plan AC format, boundaries, frontmatter conventions, classification"
      contains: "acceptance"
  key_links:
    - from: ".claude/rules/flywheel-workflow.md"
      to: "CLAUDE.md.template"
      via: "pointer reference"
      pattern: "flywheel-workflow.md"
    - from: ".claude/rules/coordination.md"
      to: "CLAUDE.md.template"
      via: "pointer reference"
      pattern: "coordination.md"
---

<objective>
Extract detailed procedural content from CLAUDE.md.template into .claude/rules/ files, preparing for compression in Plan 02.

Purpose: CLAUDE.md.template is 505 lines -- must reach less than 200 (TMPL-03). The strategy per D-15 is to merge into existing rules/ files first, only creating new files when content has no clean home.

Output: 4 rules/ files updated or created with all extracted content. CLAUDE.md.template is NOT modified in this plan -- that happens in Plan 02 after extraction targets are stable.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-template-init/07-CONTEXT.md

Source file (read in full before any edits):
@CLAUDE.md.template

Extraction targets (read in full before merging):
@.claude/rules/flywheel-workflow.md
@.claude/rules/tool-routing.md
@.claude/rules/execution-quality.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge extracted rules into existing files + create new rule files</name>
  <files>
    .claude/rules/flywheel-workflow.md
    .claude/rules/tool-routing.md
    .claude/rules/coordination.md
    .claude/rules/planning-discipline.md
  </files>
  <read_first>
    - CLAUDE.md.template (full -- the source of all content to extract)
    - .claude/rules/flywheel-workflow.md (merge target for D-17)
    - .claude/rules/tool-routing.md (merge target for D-16)
    - .claude/rules/execution-quality.md (verify D-19 is already there -- read-only)
  </read_first>
  <action>
This task extracts content from 6 sections of CLAUDE.md.template into 4 rule files. Each extraction is specified below with the EXACT content to write. Use `---` as section separators in the output files (markdown horizontal rules).

**EXTRACTION 1 -- Per D-17: Append Rules 5-7 to flywheel-workflow.md**

Keep the existing 76 lines of flywheel-workflow.md UNTOUCHED. Append a horizontal rule separator, then add these 3 new sections after the existing Etape 5:

Section "AgentDB -- archive principale par projet (Regle 5)":
- First line: "AgentDB = source de verite pour les lecons archivees, organisees par projet."
- Explain two formats: lecons projet (via /lesson migrate) and lecons cross-projet (via /ce:compound)
- Lecon projet format: [lesson:{domaine}] + Quand/Faire/Parce que/Projet/Date
- Lecon cross-projet format: [tag:{domaine}] + Contexte/Probleme/Solution/Regle/Ref
- List the 7 standard tags: lesson, skill, decision:architecture, decision:stack, lesson:error, convention:workflow, context:preference
- End with: "Ne pas sauvegarder directement dans AgentDB" and "Ne pas stocker de code ou credentials"

Section "CARL -- charger le bon domaine (Regle 6)":
- CARL charge les domaines automatiquement, ne pas charger manuellement
- Enrichir existant: apres /ce:compound si reutilisable, ajouter dans .carl/{{CARL_DOMAIN}}, 1-2 lignes max
- Creer nouveau domaine: minimum 3 regles, nommer sans tirets ({domaine}expert), ajouter RECALL keywords dans .carl/manifest
- Note: .carl/ (CARL) != .claude/skills/ (skills Claude Code)

Section "Anti-patterns contexte et memoire (Regle 7)":
- List of 9 "Ne jamais" items from CLAUDE.md.template lines 346-354, compressed:
  1. Commencer sans lire MEMORY.md et LESSONS.md
  2. Continuer une session au contexte degrade
  3. Sauvegarder dans AgentDB sans tag structure
  4. Sauvegarder directement dans AgentDB (passer par /lesson migrate ou /ce:compound)
  5. Stocker credentials dans MEMORY.md, LESSONS.md, docs/ ou AgentDB
  6. Melanger etat courant (MEMORY.md) avec lecons (LESSONS.md)
  7. Modifier CARL sans lecon capturee d'abord dans LESSONS.md
  8. Creer un domaine CARL avec moins de 3 regles
  9. Ignorer LESSONS.md lors de la planification

This adds ~60 lines. Total flywheel-workflow.md: ~136 lines.

Per D-21: The "Ce que les agents ne font PAS" section (lines 487-498 of CLAUDE.md.template) is fully duplicated by the anti-patterns in Rule 7 above. It will be removed from CLAUDE.md.template in Plan 02 (no extraction needed -- content already covered).

**EXTRACTION 2 -- Per D-16: Append Rules 3-4 to tool-routing.md**

Keep the existing 67 lines of tool-routing.md UNTOUCHED. Append a horizontal rule separator, then add:

Section "Sessions longues -- checkpoints de contexte (Regle 3)":
- 3 bullet points: signal d'alerte, utiliser /context-checkpoint, ne pas continuer degradee

Section "Chain of thought -- externaliser avant d'executer (Regle 4)":
- Trigger matrix: obligatoire quand 2+ of 5 conditions (multi-file, >50 LOC, 2+ approaches, shared interface, destructive)
- Override rule: >3 tool calls, spawn Agent, explicit request
- Skip rule: single-file <50 LOC, formatting, plan already provided
- Precedence: Override > Skip > 2-of-5
- XML format block: plan/problem/files/approach/rejected/risks (greppable via grep -q "<plan>")

This adds ~30 lines. Total tool-routing.md: ~97 lines.

**EXTRACTION 3 -- Per D-20: Create coordination.md (new file)**

Create `.claude/rules/coordination.md` with header "# Coordination GSD - Compound Engineering" and these sections:

Section "GSD lit les outputs Compound":
- 3 bullets: docs/solutions/ (ce:compound), docs/plans/ (ce:plan), docs/brainstorms/

Section "Compound lit les outputs GSD":
- ce:review reads VERIFICATION.md and SUMMARY.md
- ce:compound reads PREFLIGHT.md (findings resolus = patterns a capitaliser)

Section "Fichiers partages":
- Full table with 11 rows: LESSONS.md, docs/solutions/, docs/plans/, PLAN.md, PREFLIGHT.md, VERIFICATION.md, SUMMARY.md, MEMORY.md, DECISIONS.md, STATE.md, AgentDB
- Columns: Fichier | Producteur | Consommateurs

Total: ~35 lines.

**EXTRACTION 4 -- Per D-18 + D-22: Create planning-discipline.md (new file)**

Create `.claude/rules/planning-discipline.md` with header "# Planning Discipline -- structure des plans et documents" and these sections:

Section "Criteres d'acceptation (BDD)":
- Format: AC-N: Given/When/Then
- 5 rules: un comportement par AC, Then verifiable, pas de details d'impl, reference AC dans tache, AC = reference pour verify-work

Section "Frontieres -- tous les plans":
- Lister fichiers proteges
- Si modification necessaire: signaler deviation dans MEMORY.md

Section "Distinction .planning/ vs docs/":
- .planning/ = GSD ephemeres, docs/plans/ = CE persistants, docs/brainstorms/ = CE persistants

Section "Metadonnees documentaires":
- docs/plans/ needs title/type/status/date
- docs/brainstorms/ needs date/topic
- docs/architecture/ needs title/type/status/date (status: current ou target)
- Rules: frontmatter opens/closes with ---, dates YYYY-MM-DD

Section "Classification des donnees":
- Never store/send in Codex/AgentDB/docs/workspace: secrets, tokens, credentials, client data, prod data
- Si zero revue externe: desactiver Codex

Total: ~40 lines.

**EXTRACTION 5 -- Per D-19: Add consultation rule to planning-discipline.md**

After the Classification section, append:

Section "Regle de consultation -- debut de planification":
- 5-step numbered list:
  1. LESSONS.md deja charge (Regle #1)
  2. Lire DECISIONS.md
  3. agentdb_search avec mots-cles du domaine
  4. Si besoin: agent Explore sur docs/solutions/
  5. Integrer dans le plan
- Final line: "Pour code/infra, consulter docs/references/ via execution-quality.md Reference Layer Awareness."

This adds ~10 lines. Total planning-discipline.md: ~50 lines.

Verify that execution-quality.md already has "Reference Layer Awareness" (lines 24-36) covering the code/infra consultation. If present, no changes needed to execution-quality.md (D-19 satisfied).
  </action>
  <verify>
    <automated>
cd "/Users/gerardvinou/Claude code/Claude Projects/project-template-v2" && \
echo "=== flywheel-workflow.md ===" && \
wc -l .claude/rules/flywheel-workflow.md && \
grep -c "AgentDB" .claude/rules/flywheel-workflow.md && \
grep -c "CARL" .claude/rules/flywheel-workflow.md && \
grep -c "Anti-patterns" .claude/rules/flywheel-workflow.md && \
echo "=== tool-routing.md ===" && \
wc -l .claude/rules/tool-routing.md && \
grep -c "Chain of thought" .claude/rules/tool-routing.md && \
grep -c "checkpoint" .claude/rules/tool-routing.md && \
echo "=== coordination.md ===" && \
wc -l .claude/rules/coordination.md && \
grep -c "GSD" .claude/rules/coordination.md && \
grep -c "Compound" .claude/rules/coordination.md && \
echo "=== planning-discipline.md ===" && \
wc -l .claude/rules/planning-discipline.md && \
grep -c "BDD" .claude/rules/planning-discipline.md && \
grep -c "consultation" .claude/rules/planning-discipline.md && \
echo "=== All files exist ===" && \
test -f .claude/rules/flywheel-workflow.md && \
test -f .claude/rules/tool-routing.md && \
test -f .claude/rules/coordination.md && \
test -f .claude/rules/planning-discipline.md && \
echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - flywheel-workflow.md contains Rules 5-7 content (AgentDB, CARL, anti-patterns) per D-17
    - tool-routing.md contains Rules 3-4 content (COT trigger, context checkpoints) per D-16
    - coordination.md exists with GSD-Compound table and shared files per D-20
    - planning-discipline.md exists with AC format, boundaries, metadata, consultation per D-18, D-22, D-19
    - Original content in existing files is preserved (only appended to)
    - No content from CLAUDE.md.template lines 234-498 is lost -- each block has a destination
  </acceptance_criteria>
  <done>All 4 rules/ files contain the extracted content. flywheel-workflow.md has Rules 5-7, tool-routing.md has Rules 3-4, coordination.md has the coordination table, planning-discipline.md has plan structure + metadata + consultation rule.</done>
</task>

<task type="auto">
  <name>Task 2: Update init-project.sh to copy new rule files</name>
  <files>init-project.sh</files>
  <read_first>
    - init-project.sh (full -- need to see the "Copy rules" section around lines 156-169)
  </read_first>
  <action>
In init-project.sh, find the "Copy rules" section (around lines 156-169) and add copy commands for the 2 new rule files created in Task 1.

Add these 4 lines after the existing workflow-automation.md copy (line 169):

```bash
cp "${TEMPLATE_DIR}/.claude/rules/coordination.md" \
   "${PROJECT_DIR}/.claude/rules/coordination.md"
cp "${TEMPLATE_DIR}/.claude/rules/planning-discipline.md" \
   "${PROJECT_DIR}/.claude/rules/planning-discipline.md"
```

Also update the echo output section (around lines 272-274) to reflect 8 rules instead of 6.

Change the 3 echo lines for .claude/rules/ to 4 lines:
```bash
echo "    * .claude/rules/          (tool-routing.md + flywheel-workflow.md +"
echo "                               execution-quality.md + swarm-patterns.md +"
echo "                               model-routing.md + workflow-automation.md +"
echo "                               coordination.md + planning-discipline.md)"
```

This ensures TMPL-04/05/06/07 remain satisfied (pre-satisfied per D-04 through D-07) and that the new extraction targets from Plan 01 Task 1 get copied to new projects.
  </action>
  <verify>
    <automated>
cd "/Users/gerardvinou/Claude code/Claude Projects/project-template-v2" && \
grep -c "coordination.md" init-project.sh && \
grep -c "planning-discipline.md" init-project.sh && \
echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - init-project.sh copies coordination.md to new projects
    - init-project.sh copies planning-discipline.md to new projects
    - Echo output lists 8 rule files
    - Existing copy commands for other rules are unchanged
  </acceptance_criteria>
  <done>init-project.sh copies all 8 rules/ files including the 2 new ones created by extraction.</done>
</task>

</tasks>

<verification>
All extracted content from CLAUDE.md.template (lines 234-498) now lives in rules/ files:
- Lines 234-276 (Rules 3-4) -> tool-routing.md
- Lines 278-355 (Rules 5-7) -> flywheel-workflow.md
- Lines 358-395 (Rule 8) -> planning-discipline.md
- Lines 418-444 (Consultation) -> planning-discipline.md + execution-quality.md (already there)
- Lines 447-484 (Coordination) -> coordination.md
- Lines 487-498 (Anti-patterns) -> covered by Rule 7 in flywheel-workflow.md (D-21)
- Lines 160-189 (Metadata/classification) -> planning-discipline.md (D-22)
</verification>

<success_criteria>
- 4 rules/ files contain all extracted content with zero information loss
- init-project.sh copies the 2 new rule files
- Existing content in flywheel-workflow.md and tool-routing.md is preserved
- Plan 02 can safely remove lines 160-498 from CLAUDE.md.template knowing content has a home
</success_criteria>

<output>
After completion, create `.planning/phases/07-template-init/07-01-SUMMARY.md`
</output>
