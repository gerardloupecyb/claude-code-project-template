---
phase: 01-swarm-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/rules/swarm-patterns.md
autonomous: true
requirements:
  - SWARM-01
  - SWARM-02
  - SWARM-03
  - SWARM-04
  - SWARM-05

must_haves:
  truths:
    - "All 8 agent roles are listed with role name, responsibility, model tier, and bias"
    - "Three topology patterns (hierarchical, anti-drift, shared namespace) are documented"
    - "3-tier model routing table has Haiku/Sonnet/Opus with when-to-use and examples"
    - "SPARC phase-to-model routing table has 7 rows with a Reason column"
    - "Opus escalation triggers are listed as 5 bullet points"
    - "File is under 80 lines total"
  artifacts:
    - path: ".claude/rules/swarm-patterns.md"
      provides: "Multi-agent conventions auto-loaded by all subagents"
      contains: "architect"
  key_links:
    - from: ".claude/rules/swarm-patterns.md"
      to: ".claude/skills/pre-flight/SKILL.md"
      via: "critic role definition consumed by Phase 2"
      pattern: "critic.*Challenge.*edge cases"
    - from: ".claude/rules/swarm-patterns.md"
      to: ".claude/skills/sparc/SKILL.md"
      via: "architect + critic roles and SPARC routing consumed by Phase 3"
      pattern: "SPARC routing by phase"
---

<objective>
Rewrite `.claude/rules/swarm-patterns.md` to match the exact specification from CONTEXT.md: 8 agent roles with corrected model tiers, 3 topology patterns, 3-tier model routing, SPARC phase-to-model routing with Reason column (7 rows), expanded Opus escalation triggers as 5 bullets, and a Limits section. File must stay under 80 lines.

Purpose: This file is auto-loaded by all subagents via `.claude/rules/`. It becomes the single source of truth for multi-agent conventions referenced by pre-flight (Phase 2) and SPARC (Phase 3).
Output: Updated `.claude/rules/swarm-patterns.md`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-swarm-patterns/01-CONTEXT.md
@.claude/rules/swarm-patterns.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite swarm-patterns.md to match CONTEXT.md specification</name>
  <files>.claude/rules/swarm-patterns.md</files>
  <read_first>
    - .claude/rules/swarm-patterns.md (current state — 59 lines, needs corrections)
    - .planning/phases/01-swarm-patterns/01-CONTEXT.md (source of truth for all content)
  </read_first>
  <action>
Rewrite `.claude/rules/swarm-patterns.md` with the following exact content. The file has 4 sections.

**Section 1 — Header + Agent Roles (per D: SWARM-01)**
Title line: `# Swarm Patterns — Multi-Agent Conventions`
One-line intro: `Single source of truth for agent orchestration. Referenced by SPARC and pre-flight.`

Agent Roles table with EXACTLY these 8 rows and 4 columns (Role, Responsabilite, Model tier, Biais):

| Role | Responsabilite | Model tier | Biais |
|------|---------------|------------|-------|
| `architect` | Design systeme, APIs, boundaries | Sonnet/Opus | Propose la meilleure solution |
| `critic` | Challenge, edge cases, over-engineering | Sonnet/Opus | Cherche activement les problemes |
| `coder` | Implementation, TDD | Sonnet | Best practices |
| `reviewer` | Code review structuree | Sonnet/Opus | Qualite + securite |
| `tester` | Tests, edge cases, couverture | Sonnet | Couverture exhaustive |
| `security-auditor` | OWASP, auth, data exposure | Sonnet/Opus | Paranoid |
| `spec-writer` | Requirements, AC mesurables | Sonnet | Clarte + tracabilite |
| `logic-planner` | Pseudocode, logique, TDD anchors | Sonnet | Structure |

KEY CHANGES from current file:
- `architect` model tier: was `Opus (tier 3)` → now `Sonnet/Opus`
- `critic` model tier: was `Opus (tier 3)` → now `Sonnet/Opus`

**Section 2 — Topology and Anti-Drift (per D: SWARM-02)**
Keep the existing 3 patterns EXACTLY as they are. No changes needed:
- Pattern 1 — Hierarchical
- Pattern 2 — Anti-drift (3 bullet points)
- Pattern 3 — Shared namespace (3 bullet points)

**Section 3 — Model Routing (per D: SWARM-03 and SWARM-04)**

3-tier routing table — keep as-is (4 columns: Tier, Model, When, Examples):

| Tier | Model | When | Examples |
|------|-------|------|---------|
| 1 | Haiku | Simple task < 30% complexity | Search, formatting, grep, rename |
| 2 | Sonnet | Standard implementation (default) | Feature, bugfix, refactor, tests |
| 3 | Opus | Escalation: arbitration, security, costly decisions | Critical design, contradictions, bounded contexts |

SPARC routing table — REPLACE with 7-row table adding "Reason" column (5 columns: Phase, Agent, Model, Reason):

| Phase | Agent | Model | Reason |
|-------|-------|-------|--------|
| 1 Spec | spec-writer | Sonnet | Standard execution |
| 2 Pseudo | logic-planner | Sonnet | Standard execution |
| 3 Arch | architect | Opus | Structural decision |
| 3 Arch | critic | Opus | Challenge must match proposal tier |
| 4 Refine | implementation | Sonnet | Standard code |
| 5 Complete | reviewer | Sonnet | Ordinary review |
| 5 Complete | reviewer + critic | Opus | If security, contradiction, or bounded context |

After the SPARC table, replace the current 2-line escalation paragraph with these 5 bullet points under the heading "Escalate to Opus when:":
- Auth, security, permissions, or sensitive data
- Bounded contexts or API boundaries in scope
- Architect and critic contradict without clear resolution
- 2nd NO-GO on the same task
- Costly-to-reverse decision (schema, API contract, data model, orchestration)

**Section 4 — Limits**
Keep as-is:
- Max simultaneous agents: 6-8 standard, 12 complex systems
- Each agent returns < 200 words summary (unless file output requested)
- Never spawn an agent without verifying the previous succeeded (except explicit parallel)

**CONSTRAINT (SWARM-05):** Final file MUST be under 80 lines. Use accent characters (e, a) not unicode accents to save bytes if needed. Tables are the most token-dense format — no unnecessary prose.

**DEFERRED — Do NOT include:**
- A "use when" column in the role table
- Per-role subagent_type mapping
  </action>
  <verify>
    <automated>wc -l ".claude/rules/swarm-patterns.md" | awk '{if ($1 < 80) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (must be < 80)"}'</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "architect.*Sonnet/Opus" .claude/rules/swarm-patterns.md returns 1
    - grep -c "critic.*Sonnet/Opus" .claude/rules/swarm-patterns.md returns 1
    - grep -c "Reason" .claude/rules/swarm-patterns.md returns at least 1 (header row)
    - grep "spec-writer" .claude/rules/swarm-patterns.md shows Sonnet model
    - grep "logic-planner" .claude/rules/swarm-patterns.md shows Sonnet model
    - SPARC routing table has exactly 7 data rows (excluding header and separator)
    - Escalation section has exactly 5 bullet points starting with "- "
    - File has exactly 4 sections: Agent Roles, Topology and Anti-Drift, Model Routing, Limits
    - wc -l returns less than 80
  </acceptance_criteria>
  <done>swarm-patterns.md matches CONTEXT.md specification exactly: 8 roles with corrected tiers, 3 topologies, 3-tier + SPARC routing with Reason column, 5 escalation bullets, under 80 lines</done>
</task>

<task type="auto">
  <name>Task 2: Content completeness validation</name>
  <files>.claude/rules/swarm-patterns.md</files>
  <read_first>
    - .claude/rules/swarm-patterns.md (just written by Task 1)
    - .planning/REQUIREMENTS.md (SWARM-01 through SWARM-05 definitions)
  </read_first>
  <action>
Run a systematic verification of all 5 requirements against the file content. For each requirement, grep or count to confirm:

SWARM-01 (8 roles): Count table rows in Agent Roles section. Must find exactly: architect, critic, coder, reviewer, tester, security-auditor, spec-writer, logic-planner. Each with responsibility, model tier, and bias columns.

SWARM-02 (3 topologies): Grep for "Pattern 1", "Pattern 2", "Pattern 3". All three must exist. Verify "Hierarchical", "Anti-drift", "Shared namespace" labels.

SWARM-03 (3-tier routing): Grep for "Haiku", "Sonnet", "Opus" in the Model Routing section. Verify tier numbers 1, 2, 3.

SWARM-04 (SPARC routing): Verify the SPARC table has a "Reason" column. Count rows: must be 7 data rows. Verify Phase 3 has two rows (architect, critic both at Opus). Verify Phase 5 has two rows (Sonnet standard, Opus critical).

SWARM-05 (< 80 lines): wc -l must return < 80.

If any check fails, fix the file before proceeding.
  </action>
  <verify>
    <automated>bash -c 'FILE=".claude/rules/swarm-patterns.md"; PASS=0; FAIL=0; check() { if [ "$1" = "true" ]; then PASS=$((PASS+1)); else FAIL=$((FAIL+1)); echo "FAIL: $2"; fi }; check "$([ $(grep -c "architect\|critic\|coder\|reviewer\|tester\|security-auditor\|spec-writer\|logic-planner" "$FILE") -ge 8 ] && echo true)" "SWARM-01: 8 roles"; check "$([ $(grep -c "Pattern [123]" "$FILE") -eq 3 ] && echo true)" "SWARM-02: 3 topologies"; check "$(grep -q "Haiku" "$FILE" && grep -q "Sonnet" "$FILE" && grep -q "Opus" "$FILE" && echo true)" "SWARM-03: 3-tier routing"; check "$(grep -q "Reason" "$FILE" && echo true)" "SWARM-04: Reason column"; check "$([ $(wc -l < "$FILE") -lt 80 ] && echo true)" "SWARM-05: < 80 lines"; echo "Results: $PASS passed, $FAIL failed"'</automated>
  </verify>
  <acceptance_criteria>
    - All 5 SWARM requirements pass automated checks (0 failures)
    - grep -c "architect\|critic\|coder\|reviewer\|tester\|security-auditor\|spec-writer\|logic-planner" returns >= 8
    - grep -c "Pattern [123]" returns exactly 3
    - grep "Reason" finds the SPARC routing table header
    - wc -l returns < 80
  </acceptance_criteria>
  <done>All 5 SWARM requirements validated: SWARM-01 (8 roles present), SWARM-02 (3 topologies present), SWARM-03 (3-tier routing present), SWARM-04 (SPARC routing with Reason column), SWARM-05 (file < 80 lines)</done>
</task>

</tasks>

<verification>
Run all checks in sequence:
1. `wc -l .claude/rules/swarm-patterns.md` — must be < 80
2. `grep -c "architect\|critic\|coder\|reviewer\|tester\|security-auditor\|spec-writer\|logic-planner" .claude/rules/swarm-patterns.md` — must be >= 8
3. `grep -c "Pattern [123]" .claude/rules/swarm-patterns.md` — must be 3
4. `grep "Reason" .claude/rules/swarm-patterns.md` — must match
5. `grep "architect.*Sonnet/Opus" .claude/rules/swarm-patterns.md` — must match (not "Opus (tier 3)")
6. `grep "Escalate to Opus" .claude/rules/swarm-patterns.md` — must match
</verification>

<success_criteria>
- `.claude/rules/swarm-patterns.md` exists under 80 lines
- Contains all 8 agent roles with corrected model tiers (architect/critic = Sonnet/Opus, not Opus-only)
- Contains 3 topology patterns
- Contains 3-tier model routing table
- Contains SPARC routing table with 7 rows and Reason column
- Contains 5 Opus escalation bullet points
- No deferred features present (no "use when" column, no subagent_type mapping)
</success_criteria>

<output>
After completion, create `.planning/phases/01-swarm-patterns/01-01-SUMMARY.md`
</output>
